# General dependencies for the project
pandas==1.5.3
requests==2.28.2
boto3==1.24.16
numpy==1.23.4

# Apache Airflow dependencies (for example, if using Airflow with Docker)
apache-airflow==2.5.0
apache-airflow-providers-amazon==3.3.0
apache-airflow-providers-postgres==3.2.1

# AWS Lambda specific dependencies
aws-lambda-powertools==1.23.0

# For any additional utility libraries (e.g., for logging, testing, etc.)
python-dotenv==0.21.0
pytest==7.2.2
